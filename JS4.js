//Declare a function isPalindrome(str) that takes a string as an input.
//Return true if the given string is a palindrome. Otherwise, return false.

function isPalindrome(str) {
    let reversedString = []
    for (i=0; i<string.length; i++){
        reversedString[i] = string[string.length-1-i]

          if(string===string){
        check = 'This word' + string + 'is Palindrome'
    }else{
        check = 'This word' + string + 'isn`t Palindrome'
    }
    return check
}
const result = isPalindrome(string)
}